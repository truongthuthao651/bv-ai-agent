# =============================================================================
# Embedding + reranking + DOCX parsing — the deps the ingest/retrieval SLICE
# needs (Increment B/C). Installed in the image now.
#
# torch itself is installed separately from the PyTorch CPU
# index (CPU-only, avoids the ~5 GB CUDA stack). Do not pin torch here, or pip
# may pull the CUDA build from PyPI. FlagEmbedding drives transformers /
# tokenizers / datasets / sentence-transformers versions (see the resolved lock)
# — leave those unpinned so they don't conflict.
# =============================================================================

# bge-m3 (dense + sparse) via BGEM3FlagModel, and bge-reranker-v2-m3 via FlagReranker.
# 1.3.5 (not 1.3.3) because 1.3.3 hard-pins transformers==4.44.2, which lacks
# classes docling imports at module load (AutoModelForImageTextToText, 4.46+).
FlagEmbedding==1.3.5
# Shared with docling (requirements-pdf.txt): docling needs >=4.46, FlagEmbedding
# needs >=4.44.2; pinned to the jointly-resolved version. <5 — transformers v5
# is untested with FlagEmbedding.
transformers==4.57.6

# DOCX -> gfm+tex_math_dollars. The -binary variant BUNDLES the pandoc
# executable in the wheel (all platforms), so the native deployment
# needs no system pandoc install. The system package alternative is
# apt pandoc remains a harmless fallback.
pypandoc-binary==1.14

# XLSX -> Markdown tables (values only, merged-cell fill; xlsx_parser.py).
openpyxl==3.1.5

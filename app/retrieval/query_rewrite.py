"""Query rewriting (STUB — Phase 1 placeholder).

Rewrite a follow-up into a standalone question using chat history (local LLM),
gated by settings.enable_query_rewrite. See skill section 5.
"""

from __future__ import annotations

# TODO(phase-retrieval): implement rewrite_standalone(history, query) -> str.

"""Figure extraction (STUB — Phase 1 placeholder).

Pull figure/chart image regions out of PDFs (Docling regions) and save them under
the doc's asset folder (settings.asset_dir). Returns ``ParsedFigure`` entries.
See skill section 1.
"""

from __future__ import annotations

# TODO(phase-ingestion): implement extract_figures(doc) -> list[ParsedFigure].
